import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.irisyasmeen.hadis40',
  appName: '40 Hadis Nawawi',
  webDir: 'dist'
};

export default config;
